import Cocoa

func insertionSort(_ array:[Int]) -> [Int] {
    
    for index in 1..<array.count {
        let value = array[index]
        var position = index
        
        while (position > 0 && array[index - 1] > value) {
            //insert at the position
        }
    }
    return []
}
